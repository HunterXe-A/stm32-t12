
name: stm32-t12

on:
  watch:
    types: [started]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: install gcc for arm 
      run: sudo apt-get install gcc-arm-none-eabi -y
    - name: update
      run:apt update -y
    - name: Building
      run: |
        make
